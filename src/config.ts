const defaultConfig = Object.freeze({
  pageSizeOptions: [10, 20, 30, 40, 50],
  modalType: ['createModal', 'editModal', 'viewModal', 'deleteModal'],
  defaultErrorMessage: 'An unexpected error occurred.',
});

export default defaultConfig;
